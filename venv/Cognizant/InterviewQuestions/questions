# Vempalli Shaik Saklain – Coding Question Cheat Sheet

# 1️⃣ Basic Coding Questions
# Reverse a string without using inbuilt functions.
# Check if a string is a palindrome.
# Find the largest and smallest number in an array.
# Count the number of vowels and consonants in a string.
# Find the sum of all elements in an array.
# Print Fibonacci series up to n terms.
# Check if a number is prime.
# Find the factorial of a number using recursion.
# Find the second largest number in an array.
# Remove duplicates from an array.
# Find the GCD (HCF) of two numbers.
# Find the LCM of two numbers.
# Check if two strings are anagrams.
# Sort an array using bubble sort.
# Sort an array using selection sort.
# Find the missing number in an array of 1 to n.
# Count the frequency of each element in an array.
# Merge two sorted arrays.
# Reverse a linked list.
# Find the maximum subarray sum (basic Kadane’s algorithm).

# 2️⃣ Slightly Tricky / Basic Coding Questions
# Check if a number is a power of 2.
# Find all prime numbers up to n.
# Reverse words in a sentence.
# Remove all vowels from a string.
# Find the first non-repeating character in a string.
# Count the number of occurrences of a given element in an array.
# Rotate an array by k positions.
# Merge two sorted linked lists.
# Find the intersection of two arrays.
# Check if an array is sorted.
# Move all zeros in an array to the end.
# Find pairs in an array whose sum equals a given number.
# Find the maximum product of two numbers in an array.
# Implement a basic stack using an array or list.
# Implement a basic queue using an array or list.
# Find the missing and duplicate number in an array.
# Print all subsets of a small array.
# Count number of digits in a number.
# Find the sum of digits of a number.
# Check if a number is a palindrome.

# 3️⃣ Most Common Array / String / Number Questions
# Find the most frequent element in an array.
# Print all duplicates in an array.
# Find the longest substring without repeating characters.
# Check if two strings are rotations of each other.
# Count the number of words in a sentence.
# Check if a string has balanced parentheses.
# Find the maximum and minimum in a 2D array.
# Spiral traversal of a 2D array.
# Transpose a matrix.
# Rotate a matrix by 90 degrees.
# Find all pairs with a given difference in an array.
# Find leaders in an array (elements greater than all elements to the right).
# Implement basic string compression ("aaabb" → "a3b2").
# Reverse an array in place.
# Count the number of subarrays with sum = k.
# Find the intersection of two strings (common characters).
# Check if a number is Armstrong.
# Find the missing number in a consecutive sequence.
# Find the first repeating element in an array.
# Check if a number is a happy number.